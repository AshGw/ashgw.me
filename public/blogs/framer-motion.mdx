---
title: Framer Motion
seoTitle: Streamlining Animations with Framer Motion in React
summary: Framer Motion is a game changer
isReleased: true
isSequel: false
lastModDate: 2020-04-19T09:15:00-0401
firstModDate: 2020-04-19T09:15:00-0401
minutesToRead: 18
tags:
  - 'react'
  - 'framer-motion'
---

<H3>How It came to be</H3>
<C>
I recently stumbled upon
<L href="https://www.framer.com/motion/">Framer Motion</L>
and let me tell you, it's a game-changer when it comes to animations in React. The
variety of features bundled with it is a massive time-saver, making it incredibly
easy to implement animations  that would have otherwise been a migraine
to implement manually. In this post, I'll walk you through a couple of examples to showcase
the power of Framer Motion.

</C>
<H3>The Traditional Approach</H3>

<C>
  Let's start with a simple example: fading in a text component on view.
  Traditionally, this involves creating CSS styles for fading, handling state
  changes, and using the
  <L href="#IntersectionObserver">IntersectionObserver</L> API. It's effective but
  requires several steps and can get a bit verbose.
</C>

<Code
  code=
{`const FadingText: React.FC = () => {
  return (
    <h1 className=" text-4xl font-bold bg-gradient-to-r from-teal-500 to-indigo-500 text-transparent bg-clip-text ">
      I&apos;m Fading in
    </h1>
  );
};
`}
language="tsx"
showLineNumbers={true}

/>

<C>
  How would I usually go by doing this ?<S /> First, Create the fade in style
</C>

<Code
  code=
{`.fade-in {
  opacity: 0;
}
.fade-in.visible {
  opacity: 1;
}
`}
language="css"
showLineNumbers={true}

/>

<C>
  Then wrap the element in a parent that checks its visibility state and fades it in if it's visible.
</C>
<Code
  code=
{`const Component: React.FC = () => {
  const [isVisible, setIsVisible] = useState(false);
  return (
    <div className="flex items-center justify-center py-2">
      <div
        className={\`transition-opacity fade-in \${isVisible ? 'visible' : ''}\`}
      >
        <FadingText />
      </div>
    </div>
  );
};
`}
language="tsx"
showLineNumbers={true}

/>

<C>
  To trigger the state change when the element mounts, **`useEffect`** is needed
  with a reference to the target element:
</C>

<Code
  code={`const Component: React.FC = () => {
  const [isVisible, setIsVisible] = useState(false);
  const ref = useRef<HTMLDivElement>(null);
  useEffect(() => {
    // Handle it when in view
    setIsVisible(!isVisible);
  }, [ref]);
  return (
    <div className="flex items-center justify-center py-2">
      <div
        ref={ref}
        className={\`transition-opacity fade-in \${isVisible ? 'visible' : ''}\`}
      >
        <FadingText />
      </div>
    </div>
  );
};
`}
  language="tsx"
  showLineNumbers={true}
/>
<C>
  But I don't need the state to change when the element mounts, instead I want
  it to fade in when it comes in view, to make it happen I'll be using the
  **`IntersectionObserver`** API which basically detects when an element comes
  into or goes out of view within the browser's viewport
</C>
<Code
  id="IntersectionObserver"
  code={`const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
            // make the element invisible;
          observer.unobserve(entry.target);
        }
      },
      { threshold: 0.5 } /* trigger when at least 50% of the observed
       element is visible in the viewport. */
    );
`}
  language="tsx"
  showLineNumbers={true}
/>

<C>With this setup, the component is ready to gracefully fade in.</C>
<Code
  code={`
import React, { useEffect, useRef, useState } from 'react';
import './styles.css';
const Component: React.FC = () => {
  const [isVisible, setIsVisible] = useState(false);
  const ref = useRef<HTMLDivElement>(null);
  useEffect(() => {
    const current = ref.current;
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setIsVisible(!isVisible);
          observer.unobserve(entry.target);
        }
      },
      { threshold: 0.5 }
    );
    if (current) {
      observer.observe(current);
    }
    return () => {
      if (current) {
        observer.unobserve(current);
      }
    };
  }, [ref]);
  return (
    <div className="flex items-center justify-center py-2">
      <div
        ref={ref}
        className={\`transition-opacity fade-in \${isVisible ? 'visible' : ''}\`}
      >
        <FadingText />
      </div>
      <div className="max-h-max max-w-max overflow-y h-[10000px]">aze</div>
    </div>
  );
};
export default Component;
// fading-text.tsx
const FadingText: React.FC = () => {
    
  return (
    <h1 className=" text-4xl font-bold bg-gradient-to-r from-teal-500 to-indigo-500 text-transparent bg-clip-text ">
      I&apos;m Fading in
    </h1>
  );
};
`}
  language="tsx"
  showLineNumbers={true}
/>
<C>
  A bit cumbersome for a subtle fade-in effect, look how much of work that took,
  now I'll be doing the same except this time with
  <L href="https://www.framer.com/motion/">Framer Motion</L>
  <S />
</C>
<H3>Roided Approach</H3>
<Code
  code={`
import React from 'react';
import { motion } from 'framer-motion';
const FramerMotionFadeInComponent: React.FC = () => {
  return (
    <div className="flex justify-center items-center py-2">
      <FadingText />
    </div>
  );
};
`}
  language="tsx"
  showLineNumbers={true}
/>
<C>
  Now all I have to do is to turn any HTML element into a motion element and
  slap on the animations needed, in this case it's the component's surrounding
  div
</C>
<Code
  code={`
import React from 'react';
import { motion } from 'framer-motion';
const FramerMotionFadeInComponent: React.FC = () => {
  return (
     <div className="flex justify-center items-center py-2">
      <motion.div
        viewport={{ once: true }}
        whileInView={{
          opacity: 1,
        }}
        initial={{
          opacity: 0,
        }}
        transition={{
          duration: 1,
          ease: 'easeInOut',
        }}
      >
        <FadingText />
      </motion.div>
      {// ...}
    </div>
  );
};
`}
  language="tsx"
  showLineNumbers={true}
/>
<S3 />
<C>Resulting in</C>
<S3 />
<FramerMotionFadeInComponent />
<S3 />
<C>
  These are just a few examples, and **Framer Motion** offers more features and
  options for creating sophisticated animations in React apps. You can check out
  the <L href="https://www.framer.com/motion/introduction/">docs</L> for more
  info.
</C>
