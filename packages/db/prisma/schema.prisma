generator client {
  provider = "prisma-client-js"
}

generator markdown {
  provider = "prisma-markdown"
  output   = "./ERD.md"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
